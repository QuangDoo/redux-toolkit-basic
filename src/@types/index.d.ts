type Counter = {
  value: number;
};

type UpdateByAction = 'Nhan' | 'Chia';

type Post = {
  id: string;
  title: string;
  description: string;
  publishDate: string;
  featuredImage: string;
  published: boolean;
};
